export interface SakeItem {
  id: string;
  name: string;
  brewery: string;
  prefecture: string;
  city: string;
  price?: string;
  alcohol?: string;
  introduction?: string;
  url?: string;
  tasteScale?: string;
  tasteLabel?: string;
}
